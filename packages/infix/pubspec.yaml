name: infix
description: A lightweight Dart library that elegantly solves the code nesting problem using infix operators.
version: 2.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.14.0 <4.0.0"

dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
